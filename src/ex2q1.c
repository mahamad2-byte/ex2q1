#include <stdio.h>
int main(void) {
    float lab1, lab2, lab3, lab4, lab5;
    int penalty =0;
    float penealties_time = 0.0;
    scanf("%f %f %f %f %f", &lab1, &lab2, &lab3, &lab4, &lab5);
    int read_values = (scanf("%d %f", &penalty, $penealties_time) == 2) {
        peenalities_time = penaltuy * penalties_time;
    }
    float total_second = lab1 + l